/**
 * Constants used across the application
 */

export const DEFAULT_PORT = 3000;

export const API_ROUTES = {
  BASE: '/api',
};

export const LOGGER_SERVICE = 'api.chatbot';

export const LOGGER_FILES = {
  ERROR: 'error.log',
  COMBINED: 'combined.log',
};

export const ENV = {
  PRODUCTION: 'production',
};
